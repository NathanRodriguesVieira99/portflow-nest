_format_version: "3.0"

# ══════════════════════════════════════════════════════════════════════════════
# Services
# ══════════════════════════════════════════════════════════════════════════════
services:
  - name: container-service
    url: http://container-service:3333
    routes:
      - name: container-service-routes
        paths:
          - /container
        strip_path: true
        methods: [GET, POST, PUT, PATCH, DELETE]
    plugins:
      - name: request-transformer
        config:
          add:
            headers: ["X-Forwarded-Service:kong"]

# ══════════════════════════════════════════════════════════════════════════════
# Global Plugins
# ══════════════════════════════════════════════════════════════════════════════
plugins:
  - name: cors
    config:
      origins:
        - "*"
      methods: [GET, POST, PUT, PATCH, DELETE, OPTIONS]
      headers:
        [Accept, Content-Type, Authorization, X-Request-Id, X-Correlation-Id]

  - name: prometheus
    config:
      per_consumer: true
      status_code_metrics: true
      latency_metrics: true
      bandwidth_metrics: true
      upstream_health_metrics: true

  - name: opentelemetry
    config:
      traces_endpoint: http://jaeger:4318/v1/traces
      resource_attributes:
        service.name: kong-api-gateway
      header_type: w3c
      sampling_rate: 1.0
      propagation:
        default_format: w3c
        extract: [w3c]
        inject: [w3c]

  - name: correlation-id
    config:
      header_name: X-Correlation-Id
      generator: uuid
      echo_downstream: true
