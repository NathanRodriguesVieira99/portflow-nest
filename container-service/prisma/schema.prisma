generator client {
  provider      = "prisma-client"
  output        = "../src/container/infrastructure/persistence/database/prisma/generated"
  moduleFormat  = "cjs"
}

datasource db {
  provider = "postgresql"
}
